"""Constants for the HACS Custom Refresh integration."""

from __future__ import annotations

import logging

LOGGER = logging.getLogger(__package__)

DOMAIN = "hacs_custom_refresh"
HACS_DOMAIN = "hacs"
SERVICE_REFRESH = "refresh"
